.section-nav
  h3 In this section
  .section-flex-container
    .section-flex-item(ng-repeat="item in sectionItems")
      .section-item-header(ng-click="ctrl.redirectToSectionItem(item)") {{item.Name}}
      .section-item-description {{item.Description}}
